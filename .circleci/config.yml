version: 2
jobs:
  build:
    working_directory: ~/digital_ocean
    docker:
    - image: prorahul/symfony:nginx
    - image: mysql:5.7

    steps:
      - checkout

      - run:
          name: check nginx version
          command: nginx -v


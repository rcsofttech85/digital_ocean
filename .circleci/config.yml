version: 2
jobs:
  build:
    working_directory: ~/digital_ocean
    docker:
      - image: prorahul/symfony:latest

    steps:
      - checkout
      - run:
          name: check git version
          command: git --version

